module.exports = {
    all: () => Promise.all([
            ...require('./tasmota'),
            ...require('./zigbee'),
            ...require('./roller'),
            ...require('./433'),
            ...require('./xiaomi'),
            ...require('./dahua'),
        ].map(x => x.create()))
}

/*
let mod = moduleName => Promise.all([
    ...require(moduleName)
].map(x => x.create()))

let types = {
    tasmota: mod('./tasmota'),
    zigbee: mod('./zigbee'),
    roller: mod('./roller'),
    rf433: mod('./433'),
    xiaomi: mod('./xiaomi'),
    dahua: mod('./dahua'),
};

module.exports = {
    ...types,
    all: () => Promise.all(Object.entries(types))
}
*/